import React from "react";

const MovieList = () => {
    // render the list of movies as a grid with tailwind classes
    // generate random movies for now
  return <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    {/* Render the list of movies here */}
  </div>;
};

export default MovieList;